export interface IS3Config {
  accessKey: string;
  secretKey: string;
  endpoint: string;
  region: string;
  bucket: string;
}

export interface IApiKeysConfig {
  nftScanApiKey: string;
  blockinApiKey: string;
}
